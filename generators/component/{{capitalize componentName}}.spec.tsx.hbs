import '@testing-library/jest-dom';

import { toHaveNoViolations } from 'jest-axe';
import { describe, expect, it } from 'vitest';

import { axe, render, screen } from '../../../utils/test-utils';
import { {{capitalize componentName}} } from './{{capitalize componentName}}'

expect.extend(toHaveNoViolations);

describe('<{{capitalize componentName}} />', () => {
	const testId = 'component-{{kebabCase componentName}}-id'

	it('should render the {{capitalize componentName}}', async () => {
		const { container } = render(<{{capitalize componentName}} testId={testId}>Test</{{capitalize componentName}}>);
		expect(screen.getByText('Test')).toBeInTheDocument();

		const acessibilityResults = await axe(container);
		expect(acessibilityResults).toHaveNoViolations();
	})
})
